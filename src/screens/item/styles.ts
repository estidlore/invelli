import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  row: {
    alignItems: "center",
    flexDirection: "row",
    gap: 16,
    marginBottom: 16,
  },
  statColumn: {
    flex: 1,
    flexDirection: "column",
    gap: 4,
  },
  statRow: {
    alignItems: "center",
    flexDirection: "row",
    gap: 12,
  },
  subtitle: {
    marginBottom: 16,
  },
});

export { styles };
