import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  total: {
    borderBottomWidth: 1,
    marginBottom: 8,
    marginTop: 24,
    paddingBottom: 8,
  },
});

export { styles };
