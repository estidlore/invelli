import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "flex-start",
    borderBottomWidth: 1,
    paddingBottom: 8,
  },
});

export { styles };
