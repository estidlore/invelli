import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  allowCameraText: {
    marginBottom: 16,
  },
  camera: {
    flex: 1,
  },
  cameraWrapper: {
    flex: 1,
  },
  center: {
    alignItems: "center",
    display: "flex",
    flex: 1,
    justifyContent: "center",
  },
});

export { styles };
