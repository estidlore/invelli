import { View } from "react-native";

import { Text } from "@/components";

const SalesScreen = (): React.JSX.Element => {
  return (
    <View>
      <Text>{"Sales"}</Text>
    </View>
  );
};

export { SalesScreen };
