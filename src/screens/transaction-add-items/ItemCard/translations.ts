import { createTranslations } from "@/core/language";

const translations = createTranslations({
  ENG: {
    cost: "Cost",
    units: "Units",
  },
  SPA: {
    cost: "Costo",
    units: "Unds",
  },
});

export { translations };
