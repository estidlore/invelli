import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  searchBar: {
    display: "flex",
    flexDirection: "row",
    gap: 8,
    marginBottom: 16,
  },
});

export { styles };
