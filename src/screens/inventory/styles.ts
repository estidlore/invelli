import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  addItem: {
    marginBottom: 16,
  },
  searchBar: {
    display: "flex",
    flexDirection: "row",
    gap: 8,
    marginBottom: 16,
  },
  searchError: {
    textAlign: "center",
  },
});

export { styles };
