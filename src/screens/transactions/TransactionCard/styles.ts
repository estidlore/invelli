import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  grid: {
    alignItems: "flex-start",
    display: "flex",
    flexDirection: "row",
  },
  notes: {
    fontStyle: "italic",
  },
});

export { styles };
