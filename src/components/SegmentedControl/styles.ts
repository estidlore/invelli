import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  btn: {
    flexGrow: 1,
  },
  label: {
    marginBottom: 4,
  },
  row: {
    alignItems: "stretch",
    borderRadius: 8,
    borderWidth: 1,
    flexDirection: "row",
  },
});

export { styles };
