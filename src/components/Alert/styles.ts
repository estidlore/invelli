import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  animated: {
    borderRadius: 4,
  },
  container: {
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    gap: 8,
    paddingHorizontal: 8,
    paddingVertical: 4,
  },
});

export { styles };
