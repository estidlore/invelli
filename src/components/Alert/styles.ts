import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    borderRadius: 4,
    display: "flex",
    flexDirection: "row",
    paddingHorizontal: 8,
    paddingVertical: 4,
  },
  text: {
    marginLeft: 8,
  },
});

export { styles };
