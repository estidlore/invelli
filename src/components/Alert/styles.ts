import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  animated: {
    overflow: "hidden",
  },
  container: {
    alignItems: "center",
    borderRadius: 4,
    flexDirection: "row",
    gap: 8,
    left: 0,
    paddingHorizontal: 8,
    paddingVertical: 4,
    position: "absolute",
    top: 0,
    width: "100%",
  },
});

export { styles };
