import { StyleSheet } from "react-native";

import { colors } from "utils/colors";

const styles = StyleSheet.create({
  text: {
    color: colors.light,
    fontSize: 16,
  },
});

export { styles };
