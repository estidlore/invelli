import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  allowCameraText: {
    marginBottom: 16,
  },
  container: {
    flex: 1,
    overflow: "hidden",
  },
  rounded: {
    borderRadius: 8,
  },
  torchToggle: {
    position: "absolute",
    right: 8,
    top: 8,
  },
});

export { styles };
