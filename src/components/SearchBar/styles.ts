import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
  },
  input: {
    flexGrow: 1,
    marginHorizontal: 8,
  },
});

export { styles };
