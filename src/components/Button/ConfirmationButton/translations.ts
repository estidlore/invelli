import { createTranslations } from "@/core/language";

const translations = createTranslations({
  ENG: {
    cancel: "Cancel",
    confirm: "Confirm",
  },
  SPA: {
    cancel: "Cancelar",
    confirm: "Confirmar",
  },
});

export { translations };
