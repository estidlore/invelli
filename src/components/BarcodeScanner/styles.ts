import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  camera: {
    height: "100%",
    width: "100%"
  },
  closeButton: {
    margin: 8,
    position: "absolute",
    right: 0
  }
});

export { styles };
