import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttons: {
    justifyContent: "space-between",
    marginBottom: 0,
    marginTop: 8,
  },
  icon: {
    textAlign: "center",
    width: 24,
  },
  key: {
    marginLeft: 8,
    marginRight: 16,
  },
  row: {
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    marginBottom: 8,
  },
});

export { styles };
