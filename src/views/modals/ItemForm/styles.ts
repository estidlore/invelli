import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  codeInput: {
    flexGrow: 1,
    marginStart: 8
  },
  input: {
    marginBottom: 8
  },
  row: {
    display: "flex",
    flexDirection: "row"
  }
});

export { styles };
